[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "stegtalk"
version = "0.0.0+managed.completion"
description = "StegTalk managed completion layer"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]
