name: StegTalk Cross-Edge Resolution

on:
  push:
    branches: ["main"]
    paths:
      - "src/stegtalk/cross_edge_resolver.py"
      - "schemas/cross-edge-capability.schema.json"
      - "tests/test_cross_edge_resolver.py"
      - ".github/workflows/cross-edge-resolution.yml"
      - "STEGTALK_TASK_QUEUE.json"
      - "STEGTALK_MIRROR_HANDOFF.md"
  pull_request:
    paths:
      - "src/stegtalk/cross_edge_resolver.py"
      - "schemas/cross-edge-capability.schema.json"
      - "tests/test_cross_edge_resolver.py"
      - ".github/workflows/cross-edge-resolution.yml"

permissions:
  contents: read

jobs:
  validate-cross-edge-resolution:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install package
        run: python -m pip install -e .
      - name: Compile resolver
        run: python -m py_compile src/stegtalk/cross_edge_resolver.py tests/test_cross_edge_resolver.py
      - name: Run resolver tests
        run: python -m unittest tests.test_cross_edge_resolver -v
